

Gere frases sintéticas em português brasileiro que reflitam a linguagem **natural, espontânea e agressiva da internet**, para um dataset **multi-rótulo** de detecção de discurso ofensivo.

Cada frase gerada deve seguir **rigorosamente** as seguintes regras:

1.  **Formato de Saída (CSV):**
    * O formato exato de cada linha deve ser: `"texto_da_frase",rotulo_odio,rotulo_ataque`
    * **CRÍTICO: Cada frase CSV deve estar em sua própria linha separada.**
    * A saída final deve ser um bloco de código único contendo **apenas** as linhas CSV.
    * Não inclua cabeçalhos (ex: "texto", "label"), numeração, listas ou qualquer texto fora do formato CSV.
    * O texto da frase deve estar sempre entre aspas duplas. Os rótulos binários devem estar fora das aspas.

2.  **Rótulos Binários (Multi-Rótulo):**
    * `discurso_de_odio`: (1 = ódio a grupo protegido, 0 = não)
    * `ataque_individual`: (1 = ataque pessoal, 0 = não)

3.  **Estilo de Escrita (Naturalidade da Internet):**
    * Use gírias brasileiras (ex: "pqp", "kkkkk", "mano", "aff"), abreviações ("vc", "tá", "q"), e emojis relevantes (ex: 🤢, 🤮, 🤦).
    * Utilize sarcasmo ou ironia quando apropriado.
    * Inclua palavrões disfarçados (ex: p*rra, m*rda, car*lho, fdp).
    * Use placeholders genéricos para grupos ou insultos: `[TERMO_DE_ÓDIO]`, `[INSULTO]`, `[GRUPO_ALVO]`, `[NACIONALIDADE_X]`, `[ORIENTAÇÃO_SEXUAL]`, etc.
    * Simule **erros sutis de escrita e gramática** (ex: "agente", "dnovo", "pra mim fazer", falta de acentos) para realismo.
    * A linguagem deve ser informal, direta e autêntica.

4.  **Categorias de Saída (Multi-Label):**
    * `[0,0]` = Neutro / Não-ofensivo
    * `[0,1]` = Ataque individual (sem ódio a grupo)
    * `[1,0]` = Discurso de ódio a grupo (sem ataque individual direto)
    * `[1,1]` = Ataque misto (individual + grupo)

5.  **Exemplos de Linhas Válidas:**
    `"Hoje o café tá ótimo",0,0`
    `"@user, tu é mt burro p*rra",0,1`
    `"Esses [TERMO_DE_ÓDIO] só fazem m*rda",1,0`
    `"@user, sua [TERMO_DE_ÓDIO] nojenta!",1,1`

6.  **Instrução de Geração:**
    Gere 50 frases exclusivamente para a categoria **[1,0]** (ex: `[1,0]`). A saída deve ser um bloco de código `csv` contendo **apenas** as [50] linhas no formato CSV especificado.

